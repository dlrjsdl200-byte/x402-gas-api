# ── MGO Gas API 환경 변수 ──

# 서버 포트 (기본값: 3000)
PORT=3000

# USDC 결제를 받을 지갑 주소 (Base 네트워크)
# CDP 가입 후 발급: https://cdp.coinbase.com
WALLET_ADDRESS=YOUR_WALLET_ADDRESS_HERE

# RPC URLs (선택 — 비워두면 무료 퍼블릭 RPC 사용)
# Alchemy/Infura 키가 있으면 여기에 입력
RPC_ETHEREUM=
RPC_BASE=
RPC_ARBITRUM=
RPC_OPTIMISM=
